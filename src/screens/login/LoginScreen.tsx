import React, {memo, useEffect, useCallback} from 'react';
import {View, Text} from 'react-native';

const LoginScreen = memo((props: any) => {
  return (
    <View>
      <Text>LoginScreen</Text>
    </View>
  );
});

export default LoginScreen;
